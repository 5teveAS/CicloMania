@model IEnumerable<CicloMania.Models.mantenimientos>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Lista</h2>

<p>
    @Html.ActionLink("Crear Ficha para mantenimiento", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.cedula)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tipoMantenimiento)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.detallesArreglo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.cedula)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tipoMantenimiento)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.detallesArreglo)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.Num_Mantenimiento }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.Num_Mantenimiento }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.Num_Mantenimiento })
        </td>
    </tr>
}

</table>
